@model IEnumerable<UserTypeModel>

@inject SensorWeb.Resources.CommonLocalizationService localizer


<div class="page-title">
    <h3 class="breadcrumb-header">Cadastros</h3>
</div>


<div id="main-wrapper">
    <div class="row">

        <div class="col-md-12">

            <div class="card card-white">

                <div class="card-heading clearfix">

                    <table style="width: 100%;">
                        <tr>
                            <td><h4 class="card-title">@localizer.Get("UserTypeTitle")</h4></td>
                            <td style="text-align:right">

                                <a href="@Url.Action("Create", "UserType" )" title="@localizer.Get("Add new")">
                                    <button type="button" class="btn btn-success btn-sm"><i class="fas fa-plus"></i> </button>
                                </a>
                              

                            </td>
                        </tr>

                    </table>

                </div>

                <div class="card-body">

                    <div class="table-responsive">
                        <table id="example" class="display table" style="width:100%;text-align:center">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(modelItem => modelItem.Id)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(modelItem => modelItem.Name)
                                    </th>
                                    <th class="sccentralize">
                                        @Html.DisplayNameFor(modelItem => modelItem.UpdatedAt)
                                    </th>

                                    <th style="text-align:center">
                                        @localizer.Get("Actions")
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>

                            <tbody>
                                @foreach (var item in Model)
                                {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Id)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td class="sccentralize">
                                        @Html.DisplayFor(modelItem => item.UpdatedAtSt)
                                    </td>


                                    <td>
                                        <table style="width:100%">
                                            <tr>
                                                <td style="padding:0 4px"><a href="@Url.Action("Details", "UserType", new { id = item.Id })" title="@localizer.Get("View")"><i class="fas fa-eye"></i></a></td>
                                                <td style="padding:0 4px"><a href="@Url.Action("Edit", "UserType", new { id = item.Id })" title="@localizer.Get("Edit")"><i class="fas fa-edit" style="color:#ff6a00"></i></a></td>
                                                <td style="padding:0 4px"><a href="@Url.Action("Delete", "UserType", new { id = item.Id })" title="@localizer.Get("Delete")" style="color:#ff0000"><i class="fas fa-window-close"></i></a></td>
                                            </tr>
                                        </table>
                                    </td>

                                    <!--<td style="text-align:center">-->

                                        @*<partial name="_ListActionsControls" />*@

                                        <!--<a href="@Url.Action("Details", "UserType", new { id = item.Id })" title="@localizer.Get("View")" class="links">
                                            <img alt="List View" style="height:16px;width:23px;margin-right:15px" src="@Url.Content("~/Resources/visual.png")">
                                        </a>
                                        <a href="@Url.Action("Edit", "UserType", new { id = item.Id })" title="@localizer.Get("Edit")" class="links">
                                            <img alt="List View" style="margin-right: 15px" src="@Url.Content("~/Resources/lapis_16x16.png")">
                                        </a>

                                        <a href="@Url.Action("Delete", "UserType", new { id = item.Id })" title="@localizer.Get("Delete")" class="links">
                                            <img alt="List View" src="@Url.Content("~/Resources/exc_16x16.png")">
                                        </a>
                                    </td>
                                    <td></td>-->
                                </tr>
                                }


                                @*<tr>
                                    <td>1</td>
                                    <td>Administrador</td>
                                    <td>12/01/2022</td>
                                    <td>
                                        <table style="width:100%">
                                            <tr>
                                                <td style="padding:0 4px"><a href="#" title="detalhes"><i class="fas fa-eye"></i></a></td>
                                                <td style="padding:0 4px"><a href="#" title="editar"><i class="fas fa-edit" style="color:#ff6a00"></i></a></td>
                                                <td style="padding:0 4px"><a href="#" title="excluir" style="color:#ff0000"><i class="fas fa-window-close"></i></a></td>
                                            </tr>
                                        </table>
                                    </td>
                                </tr>*@



                            </tbody>

                        </table>
                    </div>
                </div>






            </div>
        </div>
    </div>

</div>

<!-- end page main wrapper -->
<div class="page-footer">
    <p>Iot Nest/Vibração &copy; <span class="current-year"></span>. Conteúdo de uso exclusivo.</p>
</div>